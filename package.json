{
  "name": "test-api",
  "version": "1.0.0",
  "description": "Just a practice node typescript api ",
  "main": "index.js",
  "scripts": {
    "gcp-build": "tsc",
    "lint": "eslint --ignore-path .gitignore .",
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "prestart": "npm run gcp-build",
    "start:node": "node .",
    "start": "ts-node-dev src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shagun M",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }, 
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/node": "^14.0.5",
    "ts-node-dev": "^1.0.0-pre.44",
    "tslint": "^6.1.2",
    "typescript": "^3.9.3"
  }
}