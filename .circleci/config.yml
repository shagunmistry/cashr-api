version: 2.1
orbs:
  node: circleci/node@3.0.0
jobs:
  build:
    docker:
      - image: circleci/nodejs:current
    steps:
      - checkout
      - run: npm run build
workflows:
  version: 2
  tsc_build:
    jobs:
      - build
